<?xml-stylesheet type="text/css" href="theme-dark.css"?>
<svg viewBox="0 0 900 750" xmlns="http://www.w3.org/2000/svg">
  <style>
    :root {
      --bg: #0f1419; --panel: #1a1f2e; --border: #2a3040; --header: #003366;
      --text: #e8e8e8; --text-muted: #8899aa;
      --red: #CC0000; --blue: #126BD9; --sand: #D4B98B; --green: #4ade80;
      --red-dark: #660000; --text-on-red: #ffffff; --text-on-blue: #ffffff;
    }
  </style>
  <defs>
    <clipPath id="qClip"><rect x="301" y="52" width="298" height="686"/></clipPath>
    <clipPath id="pClip"><rect x="601" y="52" width="298" height="200"/></clipPath>
    <clipPath id="cClip"><rect x="601" y="580" width="298" height="158"/></clipPath>
    <clipPath id="tClip"><rect x="601" y="498" width="298" height="80"/></clipPath>
  </defs>

  <!-- Background -->
  <rect width="900" height="750" style="fill: var(--bg)"/>

  <!-- Header bar -->
  <rect width="900" height="44" style="fill: var(--header)"/>
  <circle cx="20" cy="22" r="5" style="fill: var(--red)" id="connDot"/>
  <text x="34" y="27" font-family="monospace" font-size="13" style="fill: var(--red)" font-weight="bold">MPCR LAB</text>
  <text x="130" y="27" font-family="monospace" font-size="11" style="fill: var(--text-muted)">AI in Education: Central Tensions</text>
  <text x="520" y="27" font-family="monospace" font-size="10" style="fill: var(--text-muted)">HOST PANEL</text>
  <text x="820" y="18" font-family="monospace" font-size="9" style="fill: var(--text-muted)" id="clockDisplay">--:--:--</text>
  <text x="820" y="30" font-family="monospace" font-size="9" style="fill: var(--text-muted)" id="pollStatus">polling...</text>
  <circle cx="880" cy="22" r="4" style="fill: var(--text-muted)" id="pollDot"/>

  <!-- Vertical dividers -->
  <line x1="300" y1="44" x2="300" y2="750" style="stroke: var(--border)"/>
  <line x1="600" y1="44" x2="600" y2="750" style="stroke: var(--border)"/>

  <!-- ==================== LEFT PANEL: FLOW CONTROL (0-300) ==================== -->
  <rect x="0" y="44" width="300" height="706" style="fill: var(--panel)"/>

  <text x="150" y="68" font-family="monospace" font-size="12" style="fill: var(--sand)" font-weight="bold" text-anchor="middle">HOST CONTROLS</text>
  <line x1="16" y1="74" x2="284" y2="74" style="stroke: var(--border)"/>

  <!-- Current phase display -->
  <text x="16" y="90" font-family="monospace" font-size="9" style="fill: var(--text-muted)">CURRENT PHASE</text>
  <rect x="16" y="94" width="268" height="30" rx="4" style="fill: var(--panel); stroke: var(--border)"/>
  <text x="150" y="106" font-family="monospace" font-size="9" style="fill: var(--text-muted)" text-anchor="middle" id="phaseLabel">Phase:</text>
  <text x="150" y="118" font-family="monospace" font-size="7" style="fill: var(--text)" text-anchor="middle" id="phaseDetail">--</text>

  <!-- Phase buttons -->
  <text x="16" y="140" font-family="monospace" font-size="9" style="fill: var(--text-muted)">PHASES</text>

  <!-- Button 0: Welcome / Check-in -->
  <rect x="16" y="146" width="268" height="26" rx="4" style="fill: var(--panel); stroke: var(--border); cursor:pointer" id="btnPhase0bg"/>
  <text x="150" y="164" font-family="monospace" font-size="10" style="fill: var(--text-muted); pointer-events:none" text-anchor="middle" id="btnPhase0txt">Welcome / Check-in</text>
  <rect x="16" y="146" width="268" height="26" rx="4" fill="transparent" id="btnPhase0" style="cursor:pointer"/>

  <!-- Button 1: Tension 1 -->
  <rect x="16" y="176" width="268" height="26" rx="4" style="fill: var(--panel); stroke: var(--border); cursor:pointer" id="btnPhase1bg"/>
  <text x="150" y="194" font-family="monospace" font-size="10" style="fill: var(--text-muted); pointer-events:none" text-anchor="middle" id="btnPhase1txt">T1: Cognitive offloading</text>
  <rect x="16" y="176" width="268" height="26" rx="4" fill="transparent" id="btnPhase1" style="cursor:pointer"/>

  <!-- Button 2: Tension 2 -->
  <rect x="16" y="206" width="268" height="26" rx="4" style="fill: var(--panel); stroke: var(--border); cursor:pointer" id="btnPhase2bg"/>
  <text x="150" y="224" font-family="monospace" font-size="10" style="fill: var(--text-muted); pointer-events:none" text-anchor="middle" id="btnPhase2txt">T2: Personalization</text>
  <rect x="16" y="206" width="268" height="26" rx="4" fill="transparent" id="btnPhase2" style="cursor:pointer"/>

  <!-- Button 3: Tension 3 -->
  <rect x="16" y="236" width="268" height="26" rx="4" style="fill: var(--panel); stroke: var(--border); cursor:pointer" id="btnPhase3bg"/>
  <text x="150" y="254" font-family="monospace" font-size="10" style="fill: var(--text-muted); pointer-events:none" text-anchor="middle" id="btnPhase3txt">T3: Assistance vs. authorship</text>
  <rect x="16" y="236" width="268" height="26" rx="4" fill="transparent" id="btnPhase3" style="cursor:pointer"/>

  <!-- Button 4: Tension 4 -->
  <rect x="16" y="266" width="268" height="26" rx="4" style="fill: var(--panel); stroke: var(--border); cursor:pointer" id="btnPhase4bg"/>
  <text x="150" y="284" font-family="monospace" font-size="10" style="fill: var(--text-muted); pointer-events:none" text-anchor="middle" id="btnPhase4txt">T4: Innovation vs. governance</text>
  <rect x="16" y="266" width="268" height="26" rx="4" fill="transparent" id="btnPhase4" style="cursor:pointer"/>

  <!-- Button 5: Results -->
  <rect x="16" y="296" width="268" height="26" rx="4" style="fill: var(--panel); stroke: var(--border); cursor:pointer" id="btnPhase5bg"/>
  <text x="150" y="314" font-family="monospace" font-size="10" style="fill: var(--text-muted); pointer-events:none" text-anchor="middle" id="btnPhase5txt">Results</text>
  <rect x="16" y="296" width="268" height="26" rx="4" fill="transparent" id="btnPhase5" style="cursor:pointer"/>

  <!-- Voting toggle -->
  <line x1="16" y1="332" x2="284" y2="332" style="stroke: var(--border)"/>
  <text x="16" y="348" font-family="monospace" font-size="9" style="fill: var(--text-muted)">VOTING</text>
  <rect x="16" y="354" width="268" height="30" rx="4" style="fill: var(--panel); stroke: var(--border); cursor:pointer" id="btnVoteBg"/>
  <text x="150" y="374" font-family="monospace" font-size="11" style="fill: var(--text-muted); pointer-events:none" text-anchor="middle" font-weight="bold" id="btnVoteTxt">Open Voting</text>
  <rect x="16" y="354" width="268" height="30" rx="4" fill="transparent" id="btnVote" style="cursor:pointer"/>

  <!-- Revote button -->
  <rect x="16" y="392" width="268" height="28" rx="4" style="fill: var(--panel); stroke: var(--border); cursor:pointer" id="btnRevoteBg"/>
  <text x="150" y="411" font-family="monospace" font-size="11" style="fill: var(--text-muted); pointer-events:none" text-anchor="middle" font-weight="bold" id="btnRevoteTxt">Revote</text>
  <rect x="16" y="392" width="268" height="28" rx="4" fill="transparent" id="btnRevote" style="cursor:pointer"/>

  <!-- Countdown timer -->
  <text x="16" y="440" font-family="monospace" font-size="9" style="fill: var(--text-muted)">TIMER</text>
  <rect x="16" y="446" width="130" height="28" rx="4" style="fill: var(--panel); stroke: var(--border)"/>
  <text x="81" y="465" font-family="monospace" font-size="16" style="fill: var(--text)" text-anchor="middle" font-weight="bold" id="timerDisplay">1:30</text>

  <!-- Timer duration buttons -->
  <rect x="154" y="446" width="40" height="28" rx="4" style="fill: var(--panel); stroke: var(--border); cursor:pointer" id="btnTimer60bg"/>
  <text x="174" y="464" font-family="monospace" font-size="9" style="fill: var(--text-muted); pointer-events:none" text-anchor="middle" id="btnTimer60txt">60s</text>
  <rect x="154" y="446" width="40" height="28" rx="4" fill="transparent" id="btnTimer60" style="cursor:pointer"/>

  <rect x="198" y="446" width="40" height="28" rx="4" style="fill: var(--panel); stroke: var(--border); cursor:pointer" id="btnTimer90bg"/>
  <text x="218" y="464" font-family="monospace" font-size="9" style="fill: var(--text-muted); pointer-events:none" text-anchor="middle" id="btnTimer90txt">90s</text>
  <rect x="198" y="446" width="40" height="28" rx="4" fill="transparent" id="btnTimer90" style="cursor:pointer"/>

  <rect x="242" y="446" width="42" height="28" rx="4" style="fill: var(--panel); stroke: var(--border); cursor:pointer" id="btnTimer120bg"/>
  <text x="263" y="464" font-family="monospace" font-size="9" style="fill: var(--text-muted); pointer-events:none" text-anchor="middle" id="btnTimer120txt">120s</text>
  <rect x="242" y="446" width="42" height="28" rx="4" fill="transparent" id="btnTimer120" style="cursor:pointer"/>

  <!-- Export data button -->
  <line x1="16" y1="488" x2="284" y2="488" style="stroke: var(--border)"/>
  <rect x="16" y="496" width="268" height="28" rx="4" style="fill: var(--blue); stroke: var(--blue); cursor:pointer" id="btnExportBg"/>
  <text x="150" y="515" font-family="monospace" font-size="10" style="fill: var(--text-on-blue); pointer-events:none" text-anchor="middle" font-weight="bold" id="btnExportTxt">Download Data</text>
  <rect x="16" y="496" width="268" height="28" rx="4" fill="transparent" id="btnExport" style="cursor:pointer"/>

  <!-- Reset button -->
  <line x1="16" y1="536" x2="284" y2="536" style="stroke: var(--border)"/>
  <rect x="16" y="544" width="268" height="28" rx="4" style="fill: var(--panel); stroke: var(--red); cursor:pointer" id="btnResetBg"/>
  <text x="150" y="563" font-family="monospace" font-size="10" style="fill: var(--red); pointer-events:none" text-anchor="middle" font-weight="bold" id="btnResetTxt">Reset All</text>
  <rect x="16" y="544" width="268" height="28" rx="4" fill="transparent" id="btnReset" style="cursor:pointer"/>

  <!-- State info -->
  <text x="16" y="592" font-family="monospace" font-size="8" style="fill: var(--text-muted)" id="stateInfo">state: mpcr:ai:state</text>
  <text x="16" y="604" font-family="monospace" font-size="7" style="fill: var(--text-muted)" id="stateHash">hash: 2cb87f5f...</text>
  <text x="16" y="616" font-family="monospace" font-size="7" style="fill: var(--text-muted)" id="writeStatus">--</text>

  <!-- ==================== CENTER PANEL: QUESTIONS (300-600) ==================== -->
  <rect x="300" y="44" width="300" height="706" style="fill: var(--bg)"/>
  <rect x="301" y="44" width="298" height="30" style="fill: var(--panel)"/>
  <text x="450" y="65" font-family="monospace" font-size="12" style="fill: var(--sand)" font-weight="bold" text-anchor="middle">QUESTIONS</text>
  <text x="590" y="65" font-family="monospace" font-size="9" style="fill: var(--text-muted)" text-anchor="end" id="qCount">(0)</text>

  <!-- Questions list (clipped scrollable region) -->
  <g clip-path="url(#qClip)">
    <g id="questionsGroup" transform="translate(310, 82)"></g>
  </g>

  <!-- Scroll indicators -->
  <text x="450" y="742" font-family="monospace" font-size="8" style="fill: var(--text-muted)" text-anchor="middle" id="qScrollHint"></text>

  <!-- ==================== RIGHT PANEL: STATS (600-900) ==================== -->
  <rect x="600" y="44" width="300" height="706" style="fill: var(--bg)"/>

  <!-- Participants section -->
  <rect x="601" y="44" width="298" height="30" style="fill: var(--panel)"/>
  <text x="616" y="65" font-family="monospace" font-size="12" style="fill: var(--sand)" font-weight="bold">PARTICIPANTS</text>
  <text x="890" y="65" font-family="monospace" font-size="10" style="fill: var(--red)" text-anchor="end" id="participantCount">0</text>

  <g clip-path="url(#pClip)">
    <g id="participantsGroup" transform="translate(610, 82)"></g>
  </g>

  <!-- Vote distribution section -->
  <line x1="610" y1="256" x2="890" y2="256" style="stroke: var(--border)"/>
  <text x="616" y="274" font-family="monospace" font-size="11" style="fill: var(--sand)" font-weight="bold">VOTE DISTRIBUTION</text>
  <text x="890" y="274" font-family="monospace" font-size="9" style="fill: var(--text-muted)" text-anchor="end" id="voteTotal">0 votes</text>

  <!-- Bar chart area (dynamically rendered) -->
  <g id="voteBarsGroup" transform="translate(616, 282)"></g>

  <!-- Shift indicator -->
  <text x="616" y="478" font-family="monospace" font-size="8" style="fill: var(--text-muted)" id="shiftIndicator"></text>

  <!-- Table Notes section -->
  <line x1="610" y1="486" x2="890" y2="486" style="stroke: var(--border)"/>
  <text x="616" y="504" font-family="monospace" font-size="11" style="fill: var(--sand)" font-weight="bold">TABLE NOTES</text>

  <g clip-path="url(#tClip)">
    <g id="tableNotesGroup" transform="translate(610, 510)"></g>
  </g>

  <!-- Comments section -->
  <line x1="610" y1="580" x2="890" y2="580" style="stroke: var(--border)"/>
  <text x="616" y="598" font-family="monospace" font-size="11" style="fill: var(--sand)" font-weight="bold">RECENT COMMENTS</text>

  <g clip-path="url(#cClip)">
    <g id="commentsGroup" transform="translate(610, 610)"></g>
  </g>

  <script type="application/ecmascript"><![CDATA[
    var BASE = 'https://statevec.com';
    var ns = 'http://www.w3.org/2000/svg';

    var THEME = {
      bg: '#0f1419', panel: '#1a1f2e', border: '#2a3040', header: '#003366',
      text: '#e8e8e8', muted: '#8899aa',
      red: '#CC0000', blue: '#126BD9', sand: '#D4B98B', green: '#4ade80',
      redDark: '#660000', textOnRed: '#ffffff', textOnBlue: '#ffffff'
    };
    try {
      var cs = getComputedStyle(document.documentElement);
      var props = ['bg','panel','border','header','text','text-muted','red','blue','sand','green','red-dark','text-on-red','text-on-blue'];
      var jsKeys = ['bg','panel','border','header','text','muted','red','blue','sand','green','redDark','textOnRed','textOnBlue'];
      for (var ti = 0; ti < props.length; ti++) {
        var v = cs.getPropertyValue('--' + props[ti]).trim();
        if (v) THEME[jsKeys[ti]] = v;
      }
    } catch(e) {}

    /* -- Secrets -- */
    var STATE_SECRET = 'mpcr:ai:state';
    var STATE_HASH   = '2cb87f5f6c4c76da45361c4f934bee8e3d2ed29f0439fc35fbc1ae1727059be9';
    var CHECKIN_SECRET = 'mpcr:ai:checkins';
    var CHECKIN_HASH = 'a0c0e74f772b453b6e0352781490c9ac4dfdc781af90b4b3f24b667557c1d4e1';
    var VOTES_SECRET = 'mpcr:ai:votes';
    var VOTES_HASH   = '802e1abbe632e0612827bb2a79e8bc892a90e1b6188550cc587d0e3139763a10';
    var Q_SECRET     = 'mpcr:ai:questions';
    var Q_HASH       = '02039b6bc9f648e1ddb26a3b3d1c192c210f68fc624ad875ea2c45974f855dfc';
    var C_SECRET     = 'mpcr:ai:comments';
    var COMMENT_HASH = 'f28f9c8c5d9344c36a685ebc3817b445b16d76ae1eea257514362d953a36e2e6';
    var TABLES_SECRET = 'mpcr:ai:tables';
    var TABLES_HASH   = '4a5392639292738bfc5c0b250f822de569a507d9dcdcfb6cbd97f50ad471160c';

    /* -- Tensions -- */
    var TENSIONS = [
      'Cognitive offloading vs. deep learning',
      'Personalization vs. belonging',
      'Assistance vs. authorship',
      'Innovation vs. governance'
    ];

    /* -- State -- */
    var currentPhase = 'checkin';
    var currentTension = 0;
    var votingOpen = false;
    var revoteOpen = false;
    var activeBtn = 0;

    /* -- Cached data -- */
    var checkins = {};
    var votes = {};
    var questions = [];
    var comments = [];
    var tables = {};
    var qScrollOffset = 0;

    /* -- Timer state -- */
    var timerDuration = 90;
    var timerRemaining = 90;
    var timerRunning = false;
    var timerInterval = null;

    /* -- Reset confirmation state -- */
    var resetPrimed = false;
    var resetPrimeTimeout = null;

    /* -- DOM refs -- */
    var connDot = document.getElementById('connDot');
    var pollDot = document.getElementById('pollDot');
    var pollStatus = document.getElementById('pollStatus');
    var clockDisplay = document.getElementById('clockDisplay');
    var phaseLabel = document.getElementById('phaseLabel');
    var phaseDetail = document.getElementById('phaseDetail');
    var btnVoteBg = document.getElementById('btnVoteBg');
    var btnVoteTxt = document.getElementById('btnVoteTxt');
    var writeStatus = document.getElementById('writeStatus');
    var qCount = document.getElementById('qCount');
    var participantCount = document.getElementById('participantCount');
    var voteTotal = document.getElementById('voteTotal');
    var questionsGroup = document.getElementById('questionsGroup');
    var participantsGroup = document.getElementById('participantsGroup');
    var commentsGroup = document.getElementById('commentsGroup');
    var qScrollHint = document.getElementById('qScrollHint');
    var timerDisplay = document.getElementById('timerDisplay');
    var btnResetBg = document.getElementById('btnResetBg');
    var btnResetTxt = document.getElementById('btnResetTxt');
    var btnRevoteBg = document.getElementById('btnRevoteBg');
    var btnRevoteTxt = document.getElementById('btnRevoteTxt');
    var voteBarsGroup = document.getElementById('voteBarsGroup');
    var shiftIndicator = document.getElementById('shiftIndicator');
    var tableNotesGroup = document.getElementById('tableNotesGroup');

    /* -- Phase button refs -- */
    var phaseBgs = [];
    var phaseTxts = [];
    for (var i = 0; i < 6; i++) {
      phaseBgs.push(document.getElementById('btnPhase' + i + 'bg'));
      phaseTxts.push(document.getElementById('btnPhase' + i + 'txt'));
    }

    /* -- Clock -- */
    function updateClock() {
      var d = new Date();
      clockDisplay.textContent = d.toLocaleTimeString();
    }
    setInterval(updateClock, 1000);
    updateClock();

    /* -- Highlight active phase button -- */
    function highlightPhase(idx) {
      activeBtn = idx;
      for (var i = 0; i < 6; i++) {
        if (i === idx) {
          phaseBgs[i].setAttribute('fill', THEME.red);
          phaseBgs[i].setAttribute('stroke', THEME.red);
          phaseTxts[i].setAttribute('fill', THEME.textOnRed);
          phaseTxts[i].setAttribute('font-weight', 'bold');
        } else {
          phaseBgs[i].setAttribute('fill', THEME.panel);
          phaseBgs[i].setAttribute('stroke', THEME.border);
          phaseTxts[i].setAttribute('fill', THEME.muted);
          phaseTxts[i].setAttribute('font-weight', 'normal');
        }
      }
    }

    /* -- Update phase display -- */
    function updatePhaseDisplay() {
      if (currentPhase === 'checkin') {
        phaseLabel.textContent = 'Phase: Welcome / Check-in';
        phaseDetail.textContent = 'Participants joining...';
      } else if (currentPhase === 'results') {
        phaseLabel.textContent = 'Phase: Results';
        phaseDetail.textContent = 'Reviewing all tensions';
      } else {
        phaseLabel.textContent = 'Phase: Tension ' + (currentTension + 1);
        phaseDetail.textContent = TENSIONS[currentTension];
      }
    }

    /* -- Voting button display -- */
    function updateVoteButton() {
      if (votingOpen) {
        btnVoteBg.setAttribute('fill', THEME.blue);
        btnVoteBg.setAttribute('stroke', THEME.blue);
        btnVoteTxt.setAttribute('fill', THEME.textOnBlue);
        btnVoteTxt.textContent = 'Close Voting';
      } else {
        btnVoteBg.setAttribute('fill', THEME.panel);
        btnVoteBg.setAttribute('stroke', THEME.border);
        btnVoteTxt.setAttribute('fill', THEME.muted);
        btnVoteTxt.textContent = 'Open Voting';
      }
    }

    /* -- Revote button display -- */
    function updateRevoteButton() {
      if (revoteOpen) {
        btnRevoteBg.setAttribute('fill', THEME.sand);
        btnRevoteBg.setAttribute('stroke', THEME.sand);
        btnRevoteTxt.setAttribute('fill', THEME.bg);
        btnRevoteTxt.textContent = 'Close Revote';
      } else {
        btnRevoteBg.setAttribute('fill', THEME.panel);
        btnRevoteBg.setAttribute('stroke', THEME.border);
        btnRevoteTxt.setAttribute('fill', THEME.muted);
        btnRevoteTxt.textContent = 'Revote';
      }
    }

    /* -- Timer functions -- */
    function formatTimer(secs) {
      var m = Math.floor(secs / 60);
      var s = secs % 60;
      return m + ':' + (s < 10 ? '0' : '') + s;
    }

    function updateTimerDisplay() {
      timerDisplay.textContent = formatTimer(timerRemaining);
      if (timerRunning && timerRemaining <= 10) {
        timerDisplay.setAttribute('fill', THEME.red);
      } else if (timerRunning) {
        timerDisplay.setAttribute('fill', THEME.blue);
      } else {
        timerDisplay.setAttribute('fill', THEME.text);
      }
    }

    function startTimer() {
      if (timerInterval) clearInterval(timerInterval);
      timerRemaining = timerDuration;
      timerRunning = true;
      updateTimerDisplay();
      timerInterval = setInterval(function () {
        timerRemaining--;
        if (timerRemaining <= 0) {
          timerRemaining = 0;
          timerRunning = false;
          clearInterval(timerInterval);
          timerInterval = null;
          /* Auto-close voting or revote */
          if (votingOpen) {
            votingOpen = false;
            updateVoteButton();
            writeState();
          }
          if (revoteOpen) {
            revoteOpen = false;
            updateRevoteButton();
            writeState();
          }
        }
        updateTimerDisplay();
      }, 1000);
    }

    function stopTimer() {
      timerRunning = false;
      if (timerInterval) {
        clearInterval(timerInterval);
        timerInterval = null;
      }
      timerRemaining = timerDuration;
      updateTimerDisplay();
    }

    function setTimerDuration(secs) {
      timerDuration = secs;
      if (!timerRunning) {
        timerRemaining = secs;
      }
      updateTimerDisplay();
      highlightTimerButtons();
    }

    function highlightTimerButtons() {
      var durations = [60, 90, 120];
      for (var i = 0; i < durations.length; i++) {
        var bg = document.getElementById('btnTimer' + durations[i] + 'bg');
        var txt = document.getElementById('btnTimer' + durations[i] + 'txt');
        if (durations[i] === timerDuration) {
          bg.setAttribute('stroke', THEME.red);
          txt.setAttribute('fill', THEME.red);
        } else {
          bg.setAttribute('stroke', THEME.border);
          txt.setAttribute('fill', THEME.muted);
        }
      }
    }

    /* Timer duration button handlers */
    document.getElementById('btnTimer60').addEventListener('click', function () {
      setTimerDuration(60);
    });
    document.getElementById('btnTimer90').addEventListener('click', function () {
      setTimerDuration(90);
    });
    document.getElementById('btnTimer120').addEventListener('click', function () {
      setTimerDuration(120);
    });

    /* -- Write state to server (PUT) -- */
    async function writeState() {
      var stateObj = {
        phase: currentPhase,
        currentTension: currentTension,
        votingOpen: votingOpen,
        revoteOpen: revoteOpen,
        updatedAt: Date.now()
      };
      try {
        var r = await fetch(BASE + '/v', {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ key: STATE_SECRET, val: stateObj, ttl: 86400 })
        });
        if (r.ok) {
          writeStatus.textContent = 'state written ' + new Date().toLocaleTimeString();
          writeStatus.setAttribute('fill', THEME.green);
          connDot.setAttribute('fill', THEME.green);
        } else {
          writeStatus.textContent = 'write failed: ' + r.status;
          writeStatus.setAttribute('fill', THEME.red);
        }
      } catch (e) {
        writeStatus.textContent = 'write error: ' + e.message;
        writeStatus.setAttribute('fill', THEME.red);
        connDot.setAttribute('fill', THEME.red);
      }
    }

    /* -- Phase button handlers -- */
    document.getElementById('btnPhase0').addEventListener('click', function () {
      currentPhase = 'checkin';
      highlightPhase(0);
      updatePhaseDisplay();
      writeState();
    });
    document.getElementById('btnPhase1').addEventListener('click', function () {
      currentPhase = 'tension';
      currentTension = 0;
      highlightPhase(1);
      updatePhaseDisplay();
      writeState();
    });
    document.getElementById('btnPhase2').addEventListener('click', function () {
      currentPhase = 'tension';
      currentTension = 1;
      highlightPhase(2);
      updatePhaseDisplay();
      writeState();
    });
    document.getElementById('btnPhase3').addEventListener('click', function () {
      currentPhase = 'tension';
      currentTension = 2;
      highlightPhase(3);
      updatePhaseDisplay();
      writeState();
    });
    document.getElementById('btnPhase4').addEventListener('click', function () {
      currentPhase = 'tension';
      currentTension = 3;
      highlightPhase(4);
      updatePhaseDisplay();
      writeState();
    });
    document.getElementById('btnPhase5').addEventListener('click', function () {
      currentPhase = 'results';
      highlightPhase(5);
      updatePhaseDisplay();
      writeState();
    });

    /* -- Voting toggle -- */
    document.getElementById('btnVote').addEventListener('click', function () {
      votingOpen = !votingOpen;
      updateVoteButton();
      writeState();
      if (votingOpen) {
        startTimer();
      } else {
        stopTimer();
      }
    });

    /* -- Revote toggle -- */
    document.getElementById('btnRevote').addEventListener('click', function () {
      revoteOpen = !revoteOpen;
      updateRevoteButton();
      writeState();
      if (revoteOpen) {
        startTimer();
      } else {
        stopTimer();
      }
    });

    /* -- Reset (double-click confirmation) -- */
    document.getElementById('btnReset').addEventListener('click', function () {
      if (!resetPrimed) {
        /* First click: prime for confirmation */
        resetPrimed = true;
        btnResetBg.setAttribute('fill', THEME.redDark);
        btnResetTxt.textContent = 'Click again to confirm';
        if (resetPrimeTimeout) clearTimeout(resetPrimeTimeout);
        resetPrimeTimeout = setTimeout(function () {
          resetPrimed = false;
          btnResetBg.setAttribute('fill', THEME.panel);
          btnResetTxt.textContent = 'Reset All';
        }, 3000);
        return;
      }
      /* Second click: actually reset */
      resetPrimed = false;
      if (resetPrimeTimeout) clearTimeout(resetPrimeTimeout);
      btnResetBg.setAttribute('fill', THEME.panel);
      btnResetTxt.textContent = 'Reset All';
      currentPhase = 'checkin';
      currentTension = 0;
      votingOpen = false;
      revoteOpen = false;
      stopTimer();
      highlightPhase(0);
      updatePhaseDisplay();
      updateVoteButton();
      updateRevoteButton();
      writeState();
      // Clear all other channels
      fetch(BASE + '/v', { method: 'DELETE', headers: {'Content-Type':'application/json'}, body: JSON.stringify({key: VOTES_SECRET}) });
      fetch(BASE + '/v', { method: 'DELETE', headers: {'Content-Type':'application/json'}, body: JSON.stringify({key: Q_SECRET}) });
      fetch(BASE + '/v', { method: 'DELETE', headers: {'Content-Type':'application/json'}, body: JSON.stringify({key: C_SECRET}) });
      fetch(BASE + '/v', { method: 'DELETE', headers: {'Content-Type':'application/json'}, body: JSON.stringify({key: TABLES_SECRET}) });
    });

    /* -- Data export -- */
    document.getElementById('btnExport').addEventListener('click', async function () {
      var btnTxt = document.getElementById('btnExportTxt');
      btnTxt.textContent = 'Exporting...';
      try {
        var exportData = {
          exportedAt: new Date().toISOString(),
          state: null,
          checkins: null,
          votes: null,
          questions: null,
          comments: null,
          tables: null
        };
        var r1 = await fetch(BASE + '/v/' + STATE_HASH);
        if (r1.ok) { var d1 = await r1.json(); exportData.state = d1.val || null; }

        var r2 = await fetch(BASE + '/v/' + CHECKIN_HASH);
        if (r2.ok) { var d2 = await r2.json(); exportData.checkins = d2.val || null; }

        var r3 = await fetch(BASE + '/v/' + VOTES_HASH);
        if (r3.ok) { var d3 = await r3.json(); exportData.votes = d3.val || null; }

        var r4 = await fetch(BASE + '/v/' + Q_HASH);
        if (r4.ok) { var d4 = await r4.json(); exportData.questions = d4.val || null; }

        var r5 = await fetch(BASE + '/v/' + COMMENT_HASH);
        if (r5.ok) { var d5 = await r5.json(); exportData.comments = d5.val || null; }

        var r6 = await fetch(BASE + '/v/' + TABLES_HASH);
        if (r6.ok) { var d6 = await r6.json(); exportData.tables = d6.val || null; }

        var blob = new Blob([JSON.stringify(exportData, null, 2)], { type: 'application/json' });
        var url = URL.createObjectURL(blob);
        var a = document.createElementNS('http://www.w3.org/1999/xhtml', 'a');
        a.href = url;
        a.download = 'mpcr-ai-export-' + new Date().toISOString().replace(/[:.]/g, '-') + '.json';
        a.click();
        URL.revokeObjectURL(url);
        btnTxt.textContent = 'Download Data';
      } catch (e) {
        btnTxt.textContent = 'Export failed';
        setTimeout(function () { btnTxt.textContent = 'Download Data'; }, 2000);
      }
    });

    /* -- Render participants -- */
    function renderParticipants() {
      while (participantsGroup.firstChild) participantsGroup.removeChild(participantsGroup.firstChild);
      var keys = Object.keys(checkins);
      participantCount.textContent = keys.length;
      var roleColors = {
        Educator: THEME.blue,
        Researcher: THEME.green,
        Policymaker: THEME.sand,
        Student: THEME.red,
        educator: THEME.blue,
        researcher: THEME.green,
        policymaker: THEME.sand,
        student: THEME.red
      };
      var maxShow = 16;
      for (var i = 0; i < Math.min(keys.length, maxShow); i++) {
        var entry = checkins[keys[i]];
        var name = (entry && entry.name) ? entry.name : keys[i];
        var role = (entry && entry.role) ? entry.role : 'participant';
        var y = i * 16;

        var dot = document.createElementNS(ns, 'circle');
        dot.setAttribute('cx', 4);
        dot.setAttribute('cy', y + 6);
        dot.setAttribute('r', 3);
        dot.setAttribute('fill', roleColors[role] || THEME.muted);
        participantsGroup.appendChild(dot);

        var nameEl = document.createElementNS(ns, 'text');
        nameEl.setAttribute('x', 14);
        nameEl.setAttribute('y', y + 10);
        nameEl.setAttribute('font-family', 'monospace');
        nameEl.setAttribute('font-size', '9');
        nameEl.setAttribute('fill', THEME.text);
        nameEl.textContent = name.substring(0, 18);
        participantsGroup.appendChild(nameEl);

        var badge = document.createElementNS(ns, 'text');
        badge.setAttribute('x', 160);
        badge.setAttribute('y', y + 10);
        badge.setAttribute('font-family', 'monospace');
        badge.setAttribute('font-size', '7');
        badge.setAttribute('fill', roleColors[role] || THEME.muted);
        badge.textContent = (role || '').substring(0, 12).toUpperCase();
        participantsGroup.appendChild(badge);
      }
      if (keys.length > maxShow) {
        var more = document.createElementNS(ns, 'text');
        more.setAttribute('x', 0);
        more.setAttribute('y', maxShow * 16 + 10);
        more.setAttribute('font-family', 'monospace');
        more.setAttribute('font-size', '8');
        more.setAttribute('fill', THEME.muted);
        more.textContent = '+ ' + (keys.length - maxShow) + ' more';
        participantsGroup.appendChild(more);
      }
    }

    /* -- Render vote distribution (dual bars: initial + revised) -- */
    /* Flat keys: 'tension0:userId' = voteValue, 'revote0:userId' = revoteValue */
    function renderVotes() {
      var prefix = 'tension' + currentTension + ':';
      var revPrefix = 'revote' + currentTension + ':';
      var total = 0;
      var revTotal = 0;
      var counts = [0, 0, 0, 0, 0];
      var revCounts = [0, 0, 0, 0, 0];
      var hasRevotes = false;
      if (votes) {
        var allKeys = Object.keys(votes);
        for (var i = 0; i < allKeys.length; i++) {
          if (allKeys[i].indexOf(prefix) === 0) {
            var voteVal = votes[allKeys[i]];
            if (typeof voteVal === 'number' && voteVal >= 1 && voteVal <= 5) {
              counts[voteVal - 1]++;
              total++;
            }
          }
          if (allKeys[i].indexOf(revPrefix) === 0) {
            var revVal = votes[allKeys[i]];
            if (typeof revVal === 'number' && revVal >= 1 && revVal <= 5) {
              revCounts[revVal - 1]++;
              revTotal++;
              hasRevotes = true;
            }
          }
        }
      }
      voteTotal.textContent = total + ' votes' + (hasRevotes ? ' / ' + revTotal + ' revotes' : '');

      /* Clear existing bars */
      while (voteBarsGroup.firstChild) voteBarsGroup.removeChild(voteBarsGroup.firstChild);

      var maxBarW = 170;
      var allMax = Math.max(1, Math.max.apply(null, counts));
      if (hasRevotes) allMax = Math.max(allMax, Math.max.apply(null, revCounts));
      var barH = hasRevotes ? 7 : 12;
      var rowH = hasRevotes ? 24 : 16;

      for (var j = 0; j < 5; j++) {
        var yBase = j * rowH;

        /* Row label */
        var lbl = document.createElementNS(ns, 'text');
        lbl.setAttribute('x', 0);
        lbl.setAttribute('y', yBase + 12);
        lbl.setAttribute('font-family', 'monospace');
        lbl.setAttribute('font-size', '9');
        lbl.setAttribute('fill', THEME.muted);
        lbl.textContent = String(j + 1);
        voteBarsGroup.appendChild(lbl);

        /* Initial bar (blue) */
        var bar = document.createElementNS(ns, 'rect');
        bar.setAttribute('x', 18);
        bar.setAttribute('y', yBase + 2);
        bar.setAttribute('width', (counts[j] / allMax) * maxBarW);
        bar.setAttribute('height', barH);
        bar.setAttribute('rx', 2);
        bar.setAttribute('fill', THEME.blue);
        voteBarsGroup.appendChild(bar);

        /* Initial count */
        var ct = document.createElementNS(ns, 'text');
        ct.setAttribute('x', 200);
        ct.setAttribute('y', yBase + 10);
        ct.setAttribute('font-family', 'monospace');
        ct.setAttribute('font-size', '8');
        ct.setAttribute('fill', THEME.muted);
        ct.setAttribute('text-anchor', 'end');
        ct.textContent = counts[j];
        voteBarsGroup.appendChild(ct);

        if (hasRevotes) {
          /* Revised bar (gold) */
          var rbar = document.createElementNS(ns, 'rect');
          rbar.setAttribute('x', 18);
          rbar.setAttribute('y', yBase + 2 + barH + 1);
          rbar.setAttribute('width', (revCounts[j] / allMax) * maxBarW);
          rbar.setAttribute('height', barH);
          rbar.setAttribute('rx', 2);
          rbar.setAttribute('fill', THEME.sand);
          voteBarsGroup.appendChild(rbar);

          /* Revised count */
          var rct = document.createElementNS(ns, 'text');
          rct.setAttribute('x', 200);
          rct.setAttribute('y', yBase + 10 + barH + 1);
          rct.setAttribute('font-family', 'monospace');
          rct.setAttribute('font-size', '8');
          rct.setAttribute('fill', THEME.sand);
          rct.setAttribute('text-anchor', 'end');
          rct.textContent = revCounts[j];
          voteBarsGroup.appendChild(rct);
        }
      }

      /* Legend when revote data exists */
      if (hasRevotes) {
        var legendY = 5 * rowH + 6;
        var legRect1 = document.createElementNS(ns, 'rect');
        legRect1.setAttribute('x', 18);
        legRect1.setAttribute('y', legendY);
        legRect1.setAttribute('width', 10);
        legRect1.setAttribute('height', 7);
        legRect1.setAttribute('rx', 1);
        legRect1.setAttribute('fill', THEME.blue);
        voteBarsGroup.appendChild(legRect1);

        var legTxt1 = document.createElementNS(ns, 'text');
        legTxt1.setAttribute('x', 32);
        legTxt1.setAttribute('y', legendY + 7);
        legTxt1.setAttribute('font-family', 'monospace');
        legTxt1.setAttribute('font-size', '7');
        legTxt1.setAttribute('fill', THEME.muted);
        legTxt1.textContent = 'Initial';
        voteBarsGroup.appendChild(legTxt1);

        var legRect2 = document.createElementNS(ns, 'rect');
        legRect2.setAttribute('x', 80);
        legRect2.setAttribute('y', legendY);
        legRect2.setAttribute('width', 10);
        legRect2.setAttribute('height', 7);
        legRect2.setAttribute('rx', 1);
        legRect2.setAttribute('fill', THEME.sand);
        voteBarsGroup.appendChild(legRect2);

        var legTxt2 = document.createElementNS(ns, 'text');
        legTxt2.setAttribute('x', 94);
        legTxt2.setAttribute('y', legendY + 7);
        legTxt2.setAttribute('font-family', 'monospace');
        legTxt2.setAttribute('font-size', '7');
        legTxt2.setAttribute('fill', THEME.sand);
        legTxt2.textContent = 'Revised';
        voteBarsGroup.appendChild(legTxt2);
      }

      /* Shift indicator */
      if (hasRevotes && total > 0 && revTotal > 0) {
        var initMean = 0;
        var revMean = 0;
        for (var m = 0; m < 5; m++) {
          initMean += counts[m] * (m + 1);
          revMean += revCounts[m] * (m + 1);
        }
        initMean = initMean / total;
        revMean = revMean / revTotal;
        var shift = revMean - initMean;
        var arrow = shift > 0.05 ? '\u2191' : (shift < -0.05 ? '\u2193' : '\u2194');
        shiftIndicator.textContent = 'Shift: ' + arrow + ' ' + (shift > 0 ? '+' : '') + shift.toFixed(2) + ' (mean ' + initMean.toFixed(1) + ' \u2192 ' + revMean.toFixed(1) + ')';
        shiftIndicator.setAttribute('fill', shift > 0.05 ? THEME.blue : (shift < -0.05 ? THEME.red : THEME.muted));
      } else {
        shiftIndicator.textContent = '';
      }
    }

    /* -- Render table notes -- */
    function renderTableNotes() {
      while (tableNotesGroup.firstChild) tableNotesGroup.removeChild(tableNotesGroup.firstChild);
      if (!tables || typeof tables !== 'object') return;
      var pattern = 'tension' + currentTension;
      var tkeys = Object.keys(tables);
      var notes = [];
      for (var i = 0; i < tkeys.length; i++) {
        if (tkeys[i].indexOf(pattern) !== -1 && tkeys[i].indexOf('table') === 0) {
          /* Extract table number: "table3:tension2" -> "3" */
          var tNum = tkeys[i].replace('table', '').split(':')[0];
          notes.push({ table: tNum, text: tables[tkeys[i]] });
        }
      }
      notes.sort(function (a, b) { return parseInt(a.table) - parseInt(b.table); });
      var maxShow = 5;
      for (var j = 0; j < Math.min(notes.length, maxShow); j++) {
        var y = j * 14;
        var noteEl = document.createElementNS(ns, 'text');
        noteEl.setAttribute('x', 0);
        noteEl.setAttribute('y', y + 10);
        noteEl.setAttribute('font-family', 'monospace');
        noteEl.setAttribute('font-size', '8');
        noteEl.setAttribute('fill', THEME.text);
        var noteText = 'Table ' + notes[j].table + ': ' + String(notes[j].text || '');
        noteEl.textContent = noteText.substring(0, 42);
        tableNotesGroup.appendChild(noteEl);
      }
      if (notes.length === 0) {
        var emptyEl = document.createElementNS(ns, 'text');
        emptyEl.setAttribute('x', 0);
        emptyEl.setAttribute('y', 10);
        emptyEl.setAttribute('font-family', 'monospace');
        emptyEl.setAttribute('font-size', '8');
        emptyEl.setAttribute('fill', THEME.muted);
        emptyEl.textContent = 'No table notes yet';
        tableNotesGroup.appendChild(emptyEl);
      }
    }

    /* -- Render questions -- */
    /* FIX #3: Filter by question.tension === currentTension (numeric) */
    function renderQuestions() {
      while (questionsGroup.firstChild) questionsGroup.removeChild(questionsGroup.firstChild);
      var filtered = [];
      if (Array.isArray(questions)) {
        for (var i = 0; i < questions.length; i++) {
          if (questions[i] && questions[i].tension === currentTension) {
            filtered.push(questions[i]);
          }
        }
      } else if (questions && typeof questions === 'object') {
        var qkeys = Object.keys(questions);
        for (var k = 0; k < qkeys.length; k++) {
          var q = questions[qkeys[k]];
          if (q && q.text && q.tension === currentTension) {
            q._key = qkeys[k];
            // Count upvotes from flat 'upvote:qId:*' keys
            var upCount2 = 0;
            var upPrefix2 = 'upvote:' + qkeys[k] + ':';
            for (var uk = 0; uk < qkeys.length; uk++) {
              if (qkeys[uk].indexOf(upPrefix2) === 0 && questions[qkeys[uk]]) upCount2++;
            }
            q._upvotes = upCount2;
            filtered.push(q);
          }
        }
      }
      /* Sort by upvotes descending */
      filtered.sort(function (a, b) { return (b._upvotes || 0) - (a._upvotes || 0); });
      qCount.textContent = '(' + filtered.length + ')';
      var maxVisible = 15;
      var startIdx = qScrollOffset;
      if (startIdx > Math.max(0, filtered.length - maxVisible)) startIdx = Math.max(0, filtered.length - maxVisible);
      var endIdx = Math.min(filtered.length, startIdx + maxVisible);

      for (var j = startIdx; j < endIdx; j++) {
        var q2 = filtered[j];
        var y = (j - startIdx) * 34;
        var highlighted = q2.highlighted === true;

        /* Background */
        var bg = document.createElementNS(ns, 'rect');
        bg.setAttribute('x', 0);
        bg.setAttribute('y', y);
        bg.setAttribute('width', 276);
        bg.setAttribute('height', 30);
        bg.setAttribute('rx', 3);
        bg.setAttribute('fill', highlighted ? THEME.border : THEME.panel);
        bg.setAttribute('stroke', highlighted ? THEME.sand : THEME.border);
        questionsGroup.appendChild(bg);

        /* Text */
        var txt = document.createElementNS(ns, 'text');
        txt.setAttribute('x', 6);
        txt.setAttribute('y', y + 13);
        txt.setAttribute('font-family', 'monospace');
        txt.setAttribute('font-size', '8');
        txt.setAttribute('fill', highlighted ? THEME.sand : THEME.text);
        txt.textContent = (q2.text || '').substring(0, 38);
        questionsGroup.appendChild(txt);

        /* Upvote count */
        var uv = document.createElementNS(ns, 'text');
        uv.setAttribute('x', 240);
        uv.setAttribute('y', y + 13);
        uv.setAttribute('font-family', 'monospace');
        uv.setAttribute('font-size', '8');
        uv.setAttribute('fill', THEME.sand);
        uv.setAttribute('text-anchor', 'end');
        uv.textContent = (q2._upvotes || 0);
        questionsGroup.appendChild(uv);

        /* Author (small, below) */
        var auth = document.createElementNS(ns, 'text');
        auth.setAttribute('x', 6);
        auth.setAttribute('y', y + 24);
        auth.setAttribute('font-family', 'monospace');
        auth.setAttribute('font-size', '7');
        auth.setAttribute('fill', THEME.muted);
        auth.textContent = q2.author || '';
        questionsGroup.appendChild(auth);

        /* Star button */
        var star = document.createElementNS(ns, 'text');
        star.setAttribute('x', 262);
        star.setAttribute('y', y + 24);
        star.setAttribute('font-family', 'monospace');
        star.setAttribute('font-size', '10');
        star.setAttribute('fill', highlighted ? THEME.sand : THEME.muted);
        star.setAttribute('text-anchor', 'middle');
        star.setAttribute('style', 'cursor:pointer');
        star.textContent = '\u2605';
        star.setAttribute('data-qidx', String(j));
        star.addEventListener('click', (function (qItem) {
          return function () { highlightQuestion(qItem); };
        })(q2));
        questionsGroup.appendChild(star);
      }

      if (filtered.length > maxVisible) {
        qScrollHint.textContent = 'showing ' + (startIdx + 1) + '-' + endIdx + ' of ' + filtered.length;
      } else {
        qScrollHint.textContent = '';
      }
    }

    /* -- Highlight a question (PATCH merge) -- */
    async function highlightQuestion(q) {
      var updatedQ = {};
      for (var k in q) { if (q.hasOwnProperty(k)) updatedQ[k] = q[k]; }
      updatedQ.highlighted = !q.highlighted;
      try {
        var patchKey = q._key || null;
        if (patchKey) {
          var mergeVal = {};
          mergeVal[patchKey] = updatedQ;
          await fetch(BASE + '/v', {
            method: 'PATCH',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ key: Q_SECRET, op: 'merge', val: mergeVal })
          });
        } else if (Array.isArray(questions)) {
          /* For array-based storage, find index and rebuild */
          var idx = -1;
          for (var i = 0; i < questions.length; i++) {
            if (questions[i].text === q.text && questions[i].tension === q.tension) {
              idx = i;
              break;
            }
          }
          if (idx >= 0) {
            questions[idx].highlighted = updatedQ.highlighted;
            await fetch(BASE + '/v', {
              method: 'PUT',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ key: Q_SECRET, val: questions, ttl: 86400 })
            });
          }
        }
        pollAll();
      } catch (e) {
        writeStatus.textContent = 'star error: ' + e.message;
        writeStatus.setAttribute('fill', THEME.red);
      }
    }

    /* -- Render comments -- */
    /* FIX #4: Filter by comment.tension === currentTension (numeric) */
    function renderComments() {
      while (commentsGroup.firstChild) commentsGroup.removeChild(commentsGroup.firstChild);
      var list = [];
      if (Array.isArray(comments)) {
        for (var ci = 0; ci < comments.length; ci++) {
          if (comments[ci] && comments[ci].tension === currentTension) {
            list.push(comments[ci]);
          }
        }
        list = list.slice(-5).reverse();
      } else if (comments && typeof comments === 'object') {
        var ck = Object.keys(comments);
        for (var i = 0; i < ck.length; i++) {
          var cm = comments[ck[i]];
          if (cm && cm.tension === currentTension) {
            list.push(cm);
          }
        }
        list.sort(function (a, b) { return (b.t || 0) - (a.t || 0); });
        list = list.slice(0, 5);
      }
      for (var j = 0; j < list.length; j++) {
        var c = list[j];
        var y = j * 22;
        var nameEl = document.createElementNS(ns, 'text');
        nameEl.setAttribute('x', 0);
        nameEl.setAttribute('y', y + 10);
        nameEl.setAttribute('font-family', 'monospace');
        nameEl.setAttribute('font-size', '8');
        nameEl.setAttribute('fill', THEME.red);
        nameEl.textContent = (c.author || c.name || c.from || 'anon').substring(0, 10);
        commentsGroup.appendChild(nameEl);

        var textEl = document.createElementNS(ns, 'text');
        textEl.setAttribute('x', 70);
        textEl.setAttribute('y', y + 10);
        textEl.setAttribute('font-family', 'monospace');
        textEl.setAttribute('font-size', '8');
        textEl.setAttribute('fill', THEME.text);
        textEl.textContent = (c.text || '').substring(0, 28);
        commentsGroup.appendChild(textEl);
      }
    }

    /* -- Scroll questions via keyboard -- */
    document.addEventListener('keydown', function (e) {
      if (e.key === 'ArrowDown') { qScrollOffset++; renderQuestions(); }
      if (e.key === 'ArrowUp' && qScrollOffset > 0) { qScrollOffset--; renderQuestions(); }
    });

    /* -- Poll all endpoints -- */
    async function pollAll() {
      pollDot.setAttribute('fill', THEME.muted);
      pollStatus.textContent = 'polling...';
      var ok = true;

      /* State */
      try {
        var r1 = await fetch(BASE + '/v/' + STATE_HASH);
        if (r1.ok) {
          var d1 = await r1.json();
          if (d1.val) {
            currentPhase = d1.val.phase || 'checkin';
            currentTension = d1.val.currentTension || 0;
            votingOpen = d1.val.votingOpen || false;
            revoteOpen = d1.val.revoteOpen || false;
            /* Sync button highlight */
            if (currentPhase === 'checkin') highlightPhase(0);
            else if (currentPhase === 'results') highlightPhase(5);
            else highlightPhase(currentTension + 1);
            updatePhaseDisplay();
            updateVoteButton();
            updateRevoteButton();
          }
        }
      } catch (e) { ok = false; }

      /* Check-ins */
      try {
        var r2 = await fetch(BASE + '/v/' + CHECKIN_HASH);
        if (r2.ok) {
          var d2 = await r2.json();
          if (d2.val) {
            checkins = d2.val;
            renderParticipants();
          }
        }
      } catch (e) { ok = false; }

      /* Votes */
      try {
        var r3 = await fetch(BASE + '/v/' + VOTES_HASH);
        if (r3.ok) {
          var d3 = await r3.json();
          if (d3.val) {
            votes = d3.val;
            renderVotes();
          }
        }
      } catch (e) { ok = false; }

      /* Questions */
      try {
        var r4 = await fetch(BASE + '/v/' + Q_HASH);
        if (r4.ok) {
          var d4 = await r4.json();
          if (d4.val) {
            questions = d4.val;
            renderQuestions();
          }
        }
      } catch (e) { ok = false; }

      /* Comments */
      try {
        var r5 = await fetch(BASE + '/v/' + COMMENT_HASH);
        if (r5.ok) {
          var d5 = await r5.json();
          if (d5.val) {
            comments = d5.val;
            renderComments();
          }
        }
      } catch (e) { ok = false; }

      /* Tables */
      try {
        var r6 = await fetch(BASE + '/v/' + TABLES_HASH);
        if (r6.ok) {
          var d6 = await r6.json();
          if (d6.val) {
            tables = d6.val;
            renderTableNotes();
          }
        }
      } catch (e) { ok = false; }

      if (ok) {
        pollDot.setAttribute('fill', THEME.green);
        connDot.setAttribute('fill', THEME.green);
        pollStatus.textContent = 'connected';
      } else {
        pollDot.setAttribute('fill', THEME.red);
        pollStatus.textContent = 'error';
      }
    }

    /* -- Init -- */
    highlightPhase(0);
    updatePhaseDisplay();
    updateVoteButton();
    updateRevoteButton();
    highlightTimerButtons();
    updateTimerDisplay();
    pollAll();
    setInterval(pollAll, 4000);
  ]]></script>
</svg>
